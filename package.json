{
  "name": "gut-tracker",
  "private": true,
  "description": "Scripts de conveniencia para levantar API y App desde la raíz",
  "scripts": {
    "dev:api": "cd api && PYTHONPATH=. FLASK_APP=app:app pipenv run dev",
    "dev:app": "cd app && npm run dev",
    "dev": "concurrently -n api,app -c blue,green \"npm run dev:api\" \"npm run dev:app\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
